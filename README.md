# FASTMINDS Website

## Build CSS

```bash
npm install
npm run build
```

## Development

```bash
npm run watch:css
```

## Deploy to Hostinger

Upload all files except `node_modules/`, `src/`, `tailwind.config.js`, `package.json`, `package-lock.json`.

The compiled CSS is in `assets/css/style.css`.
